package Main;

import java.io.IOException;

import org.xml.sax.*; //API for XML read only from XML file
import org.w3c.dom.*;//read XML directly to the memory nodes store as object

//able to write back
import javax.xml.parsers.*;//get xml elements and store as dom object

public class menu {
	public static void main(String[] args) {
		System.out.println("this is main menu!");
		// should be a fully menu when ever you make change you should commit
		// the file
		// at the end of the day you have to push from the local repository to
		// the remote

		
	}
}
